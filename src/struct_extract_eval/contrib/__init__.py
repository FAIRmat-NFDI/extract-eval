"""Domain-specific comparators and utilities.

Each subpackage provides comparators tailored to a specific domain.
Install the corresponding optional extra to get the dependencies:

    pip install struct-extract-eval[materials]
"""
